# —— 二进制运行时 / 依赖（可从 fnOS SDK 重新获取或用 npm 安装；fpk 已内嵌）——
# Linux x64 Node.js v22 运行时，121MB，超出 GitHub 100MB 单文件限制
app/server/node
node_modules/
app/server/node_modules/

# —— 运行时数据（本机数据，不入库）——
*.log
*.pid
*.sock
data/
calendars.json

# —— 系统 / 编辑器杂项 ——
.DS_Store
Thumbs.db
.idea/
.vscode/
*.swp
